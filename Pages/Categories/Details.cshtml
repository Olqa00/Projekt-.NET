@page
@model Projekt_2.Pages.Categories.DetailsModel

@{
    ViewData["Title"] = "Szczegóły kategorii";
}

<h1>Szczegóły</h1>

<div>
    <h4>Kategoria</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Category.name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Category.name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Category.ID">Edytuj</a> |
    <a asp-page="./Index">Powrót</a>
</div>
